/**
 * Re-export from the shared components/admin/ location.
 * The component was moved to avoid extending the plugin-boundary exception
 * (core pages now use TeamMultiSelect for standard user forms too).
 */
export { TeamMultiSelect } from "@/components/admin/TeamMultiSelect";
