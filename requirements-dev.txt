# Development / CI dependencies (NOT installed in the production Docker image).
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Lint
ruff==0.15.20

# Dev convenience (shell_plus, runserver_plus, etc.) - stripped from
# INSTALLED_APPS in production via config/settings_production.py.
django-extensions==4.1
